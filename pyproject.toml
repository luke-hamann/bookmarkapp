[project]
name = "bookmarkapp"
version = "1.0"
description = "A web app for managing bookmarks"
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
